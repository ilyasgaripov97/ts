import { log } from "console";

const s: string = "example";
const n: number = 5;
const b: boolean = true;

log(s, n, b);

const numbers: number[] = [1, 2, 3];
const strings: string[] = ["aa", "bb", "cc"];
const booleans: boolean[] = [false, false, true];

log(numbers, strings, booleans);